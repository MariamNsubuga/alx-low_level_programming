#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H
/*
 * Header file that defines a function like macro ABS(x) 
 * compuutes absolute value of a number x.
 */
#define ABS(x)  ((X) < 0 ? (-x) : (x))

#endif
